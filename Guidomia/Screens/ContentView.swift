//
//  ContentView.swift
//  Guidomia
//
//  Created by Mark Valles on 7/3/24.
//

import SwiftUI

struct ContentView: View {
  var body: some View {
    VStack {
      ZStack {
        
      }
      .frame(maxWidth: .infinity)
      
      Image(systemName: "globe")
        .imageScale(.large)
        .foregroundStyle(.tint)
      Text("Hello, world!")
    }
    .padding()
  }
}

#Preview {
  ContentView()
}
